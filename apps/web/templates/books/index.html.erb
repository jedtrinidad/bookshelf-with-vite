<h1 class="font-bold text-2xl">Books</h1>

<% if books.any? %>
  <%= books_list %>
<% else %>
  <p>There are no books yet.</p>
  <div class="bg-gray-100 mt-3 p-3 max-w-md rounded">
    <%= add_book_form %>
  </div>
<% end %>